# URL publique de l'API (utilise le proxy interne Next.js par defaut)
NEXT_PUBLIC_API_URL=/api/v1

# URL interne du serveur backend FastAPI pour le SSR et les routes API proxy
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
